accounts
========

The `accounts` app handles user authentication, user management, and email-related functionality for the AppTrack project.

.. toctree::
   :maxdepth: 2
   :caption: Submodules:

   accounts.backends
   accounts.forms
   accounts.mail
   accounts.managers
   accounts.messages
   accounts.models
   accounts.views
